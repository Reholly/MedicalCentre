<Window x:Class="MedicalCentre.Windows.DoctorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicalCentre.Windows"
        mc:Ignorable="d"
        Title="Окно Врачебного персонала" Height="450" Width="800" Background="Azure">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
                      x:Name="Splitter"/>
        <Menu Grid.ColumnSpan="3" VerticalAlignment="Top" Height="17.5">
            <MenuItem Header="View">
                <MenuItem Header="Today's appointments" Command="{Binding ShowTodaysAppointmentsCommand}"/>
                <MenuItem Header="All patients" Command="{Binding ShowAllPatientsCommand}"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="Input" x:Name="InputHelpButton" Command="{Binding ShowInputHelpCommand}"/>
            </MenuItem>
        </Menu>
        <Frame Grid.Column="2" Margin="0 17.5 0 0" Name="DoctorRightFrame" />
        <Frame Grid.Column="0" Margin="0 17.5 0 0" Name="DoctorLeftFrame"/>
    </Grid>
</Window>