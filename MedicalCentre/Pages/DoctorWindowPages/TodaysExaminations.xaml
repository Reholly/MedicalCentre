<Page x:Class="MedicalCentre.Pages.DoctorWindowPages.TodaysExaminations"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedicalCentre.Pages.DoctorWindowPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Today's Examinations" BackGround="Azure">
    <Grid>
        <DataGrid x:Name="todaysPatients" Grid.Column="0" AutoGenerateColumns="True"
                  Background="Azure" Margin="0 17.5 0 0" CanUserAddRows="False" CanUserDeleteRows="True" 
                  CanUserReorderColumns="True" ItemsSource="{Binding Examinations}"/>
        <Menu Height="17.5" VerticalAlignment="Top">
            <MenuItem Header="Add Row" Command="{Binding AddExaminationCommand}"/>
        </Menu>
    </Grid>
</Page>